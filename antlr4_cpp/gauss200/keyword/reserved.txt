ALL 保留
ANALYSE 保留
ANALYZE 保留
AND 保留
ANY 保留
ARRAY 保留
ASC 保留
ASYMMETRIC 保留
AS 保留
AUTHID 保留
BOTH 保留
BUCKETS 保留
CASE 保留
CAST 保留
CHECK 保留
COLLATE 保留
COLUMN 保留
CONSTRAINT 保留
CREATE 保留
CURRENT_CATALOG 保留
CURRENT_DATE 保留
CURRENT_ROLE 保留
CURRENT_TIMESTAMP 保留
CURRENT_TIME 保留
CURRENT_USER 保留
DEFAULT 保留
DEFERRABLE 保留
DESC 保留
DISTINCT 保留
DO 保留
ELSE 保留
END 保留
EXCEPT 保留
FALSE 保留
FETCH 保留
FOREIGN 保留
FOR 保留
FROM 保留
FUNCTION 保留
GRANT 保留
GROUP 保留
HAVING 保留
INITIALLY 保留
INTERSECT 保留
INTO 保留
IN 保留
IS 保留
LEADING 保留
LESS 保留
LIMIT 保留
LOCALTIMESTAMP 保留
LOCALTIME 保留
MAXVALUE 保留
MINUS 保留
MODIFY 保留
NLSSORT 保留
NOT 保留
NULL 保留
OFFSET 保留
ONLY 保留
ON 保留
ORDER 保留
OR 保留
PERFORMANCE 保留
PLACING 保留
PRIMARY 保留
PROCEDURE 保留
REFERENCES 保留
REJECT 保留
RETURNING 保留
RETURN 保留
SELECT 保留
SESSION_USER 保留
SOME 保留
SPLIT 保留
SYMMETRIC 保留
SYSDATE 保留
TABLE 保留
THEN 保留
TO 保留
TRAILING 保留
TRUE 保留
UNION 保留
UNIQUE 保留
USER 保留
USING 保留
VARIADIC 保留
WHEN 保留
WHERE 保留
WINDOW 保留
WITH 保留

